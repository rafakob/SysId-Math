<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- ActionBar and Tabs background color -->
        <item name="colorPrimary">@color/toolbar_primary</item>
        <!-- ActionBar and Tabs text color -->
        <item name="android:textColorPrimary">@color/toolbar_text</item>
        <!-- Main window background -->
        <!--<item name="android:windowBackground">@color/window_background</item>-->
        <!--<item name="android:colorBackground">@color/window_background</item>-->
        <!--<item name="android:background">@color/window_background</item>-->

        <!-- Removes actionbar shadow -->
        <item name="android:windowContentOverlay">@null</item>

    </style>

    <style name="PreferenceScreen" parent="AppTheme">
        <item name="android:textColor">@drawable/preftext_color</item>

    </style>




</resources>
